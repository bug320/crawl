＃说明
　直接把这个文件解压到工程目录，带文件夹
　form bug320 import *
	应该就能使用了这个带有缓存机制的下载类，可以如果不想实现缓存，在　downloader.py 中，在生成　Downloader 类的对象时候可以直接设置　cache = None ,就禁用了缓存功能，
　也可以自己定义个一个新的　cache ,但是必须那要设置　__getitem__ 和　__setitem__　接口，当然，不设置这个接口，就要修改　Downloader 类了。具体例子可以看源码的测试。如果报错thrift相关的错误，可以把　DBS 下的　hbase 文件夹替换成本地生　thrift 生成的 hbase 文件夹。还有错误请联系我
	Downloader 类用法:
	download = Downloader() # 这样就是默认了，带有缓存机制，默认设置可以看　downloade.py 文件，上面的默认设置可以自己定义下，然后就不用到处修改了。
	html = downloade(url)   # 这就得到了 html 网页
	“”“　下面可以自己去对网页解析
	”“”
	
	虽然设置的功能比较多，但现在暂时我都没有用到，设置这些是为了以后扩展，目前我自己常用的是　下载延时　和 下载缓存，这样设计的时候，我可以把　存进　hbase 和　mysql　完全分开写，同时避免对同一条ｕｒｌ两次存都要打开。这样　对于同一个 url 我每次都只需要下载一次。
	原本打算添加一个定时删除功能，比方说过　把超过　30 天前的缓存删除，避免越来越大。但是我不知道，怎么在　python 中对 hbase 的表进行删除操作。所以，谁能好了这一块，可以给我说下。也是希望大家一起来看看这个写的那个地方需要修改。我觉得这个类还是不完善的，我的最初是像把断点功能也加上的，不过没成功，就先分开了。只是完成了缓存功能。
	注: 缓存是指下载网页的时候，以url 为键值，对应的　网页源码为　value 存储到　数据库或者本地，重新下载这个网页的时候不先从服务器请求下载，而是先检测本地或者数据库是否有缓存，如果有返回本地存储的结果，如果没有在去请求服务器下载网页。（设置字典接口就是为了操作方便）
	英文注释有错记得和说下，这个英文注释是因为我有时候在手机上写，在电脑打开中文乱码所以我一般写注释优先英文。可怜我英语四级始终没过。。。
	

